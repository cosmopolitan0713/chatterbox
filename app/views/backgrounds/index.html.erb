<%= render "shared/header" %>
<div class="container">
  <% @backgrounds.each do |background| %>
   <div class="background-item">
      <%= link_to image_tag(background.image, class: 'background-image'), edit_background_path(background) %>
      <p><%= background.name %></p>
    </div>
  <% end %>
</div>